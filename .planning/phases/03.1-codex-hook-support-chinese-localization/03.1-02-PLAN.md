---
phase: "03.1"
plan: "02"
type: execute
wave: 2
depends_on:
  - "03.1-01"
files_modified:
  - src/notifier/core/settings.py
  - src/notifier/cli/config.py
  - src/notifier/tests/test_settings.py
  - src/notifier/tests/test_cli_config.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Codex hooks install at user level by default so all local Codex projects can be monitored (D-15)"
    - "Codex install manages only notifier-owned hook entries and preserves unrelated Codex hooks/settings (D-16)"
    - "notifier-config install installs both Claude Code and Codex hooks and reports both statuses (D-17)"
    - "Codex uninstall is not implemented in this phase; leave lifecycle reversal to Phase 5 (D-18)"
    - "Hook commands use the provider-aware CLI from 03.1-01 and invoke Codex events with provider codex (D-01, D-03)"
    - "CLI install/config output is Chinese-first for both providers, using concise status text while keeping provider names stable (D-11, D-12, D-13, D-14)"
  artifacts:
    - path: "src/notifier/core/settings.py"
      provides: "Claude Code and Codex hook installation helpers with non-destructive merge behavior"
      exports:
        - "install_hooks"
        - "install_codex_hooks"
    - path: "src/notifier/cli/config.py"
      provides: "Unified install command with provider-specific Chinese status output"
      contains: "Codex"
    - path: "src/notifier/tests/test_settings.py"
      provides: "Config preservation and idempotency tests for Claude Code and Codex hook install"
      contains: "codex"
  key_links:
    - from: "src/notifier/cli/config.py"
      to: "src/notifier/core/settings.py"
      via: "install_hooks and install_codex_hooks"
      pattern: "install_codex_hooks"
    - from: "src/notifier/core/settings.py"
      to: "src/notifier/cli/hook.py"
      via: "hook command python -m notifier.cli.hook <event> codex"
      pattern: "notifier.cli.hook"
---

<objective>
Codex Hook Installation + Unified Config CLI

Add user-level Codex hook installation alongside the existing Claude Code hook installer. Preserve unrelated user Codex configuration, install notifier-owned Codex hooks for the relevant lifecycle events, and make `notifier-config install` install both providers with Chinese-first status output.

Purpose: make Codex support adoptable without manual config editing or destructive settings changes.
Output: tested Codex install helpers and unified install CLI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.1-codex-hook-support-chinese-localization/03.1-CONTEXT.md
@.planning/phases/03.1-codex-hook-support-chinese-localization/03.1-RESEARCH.md
@.planning/phases/03.1-codex-hook-support-chinese-localization/03.1-01-PLAN.md

<interfaces>
This plan depends on Plan 01:
- `src/notifier/cli/hook.py` must accept provider `codex`.
- Codex event classification must exist for `SessionStart`, `PermissionRequest`, and `Stop`.

External Codex hook docs:
- User-level hook config is supported under the Codex user config layer.
- Command hooks receive JSON over stdin.

Existing Claude install pattern:
- `install_hooks()` preserves unrelated settings.
- `_is_notifier_entry()` detects notifier-owned hook commands.
- `_write_json()` atomically writes JSON.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Codex hook entry builders</name>
  <files>
    src/notifier/core/settings.py (modify)
    src/notifier/tests/test_settings.py (modify)
  </files>
  <read_first>
    - src/notifier/core/settings.py
    - src/notifier/tests/test_settings.py
    - 03.1-CONTEXT.md D-15, D-16
    - 03.1-RESEARCH.md Codex Hooks
  </read_first>
  <action>
    Add Codex-specific hook configuration constants and builders.

    Step 1: Add user-level Codex path constants:
    ```python
    CODEX_HOOKS_FILE = Path.home() / ".codex" / "hooks.json"
    CODEX_OWNERSHIP_FILE = Path.home() / ".codex" / ".notifier-ownership.json"
    ```

    Step 2: Add `_build_codex_hook_entries()` that uses `sys.executable` and points to the provider-aware CLI:
    ```python
    f'"{python}" -m notifier.cli.hook SessionStart codex'
    f'"{python}" -m notifier.cli.hook PermissionRequest codex'
    f'"{python}" -m notifier.cli.hook Stop codex'
    ```

    Step 3: Use the Codex event names as top-level hook keys unless implementation research discovers the local Codex config shape needs a nested `hooks` key. Preserve the official Codex hook shape.

    Step 4: Add a `CODEX_OWNERSHIP_MARKER` with tool/version/installed_at and event types.

    Step 5: Add tests asserting Codex hook entries include `SessionStart`, `PermissionRequest`, and `Stop`, all commands include `notifier.cli.hook`, and all commands include `codex`.
  </action>
  <verify>
    <automated>pytest src/notifier/tests/test_settings.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Codex hook entries cover `SessionStart`, `PermissionRequest`, and `Stop`.
    - Every Codex hook command passes provider argument `codex`.
    - Codex path constants default under `Path.home() / ".codex"`.
    - Claude Code hook entries remain unchanged except for provider-aware CLI compatibility if needed.
  </acceptance_criteria>
  <done>
    Codex hook entry builders exist and are covered by tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement non-destructive Codex hook installation</name>
  <files>
    src/notifier/core/settings.py (modify)
    src/notifier/tests/test_settings.py (modify)
  </files>
  <read_first>
    - src/notifier/core/settings.py
    - src/notifier/tests/test_settings.py
    - 03.1-CONTEXT.md D-15, D-16, D-18
  </read_first>
  <action>
    Implement `install_codex_hooks()` beside the existing Claude Code `install_hooks()`.

    Required function shape:
    ```python
    def install_codex_hooks(
        hooks_path: Optional[Path] = None,
        ownership_path: Optional[Path] = None,
    ) -> bool:
        ...
    ```

    Implementation requirements:
    - Read existing Codex hooks JSON with `_read_json`.
    - Remove only notifier-owned Codex hook entries, using `_is_notifier_entry`.
    - Preserve unrelated hook entries and unrelated settings.
    - Merge fresh Codex hook entries.
    - Write Codex ownership marker to `.codex/.notifier-ownership.json`.
    - Be idempotent: running twice does not duplicate notifier hooks.
    - Do not implement uninstall.

    Tests to add:
    - Creates new Codex hooks file if missing.
    - Preserves unrelated Codex settings.
    - Preserves unrelated Codex hook entries.
    - Adds all Codex event types.
    - Creates ownership file.
    - Running install twice does not duplicate notifier entries.
    - Corrupt Codex hook JSON is handled the same way current Claude settings handling works.
  </action>
  <verify>
    <automated>pytest src/notifier/tests/test_settings.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `install_codex_hooks()` returns True on successful install.
    - Existing user Codex hooks/settings survive install.
    - Existing notifier-owned Codex hooks are replaced rather than duplicated.
    - Codex ownership file is written.
    - No uninstall function or CLI command is introduced in this phase.
  </acceptance_criteria>
  <done>
    Codex hooks install non-destructively and idempotently.
  </done>
</task>

<task type="auto">
  <name>Task 3: Make notifier-config install unified</name>
  <files>
    src/notifier/cli/config.py (modify)
    src/notifier/tests/test_cli_config.py (create or modify)
  </files>
  <read_first>
    - src/notifier/cli/config.py
    - src/notifier/core/settings.py
    - existing tests under src/notifier/tests
    - 03.1-CONTEXT.md D-17
  </read_first>
  <action>
    Update the user-facing install command.

    Step 1: Import `install_codex_hooks`, `CODEX_HOOKS_FILE`, and `CODEX_OWNERSHIP_FILE`.

    Step 2: Keep the command name `install`.

    Step 3: Add optional test-friendly paths:
    - `--settings` and `--ownership` keep existing Claude behavior.
    - Add `--codex-hooks` and `--codex-ownership` for tests and advanced use.

    Step 4: `install()` should call both:
    ```python
    install_hooks(...)
    install_codex_hooks(...)
    ```

    Step 5: Print Chinese-first provider-specific status:
    ```text
    Claude Code hooks: 已安装 (...)
    Codex hooks: 已安装 (...)
    ```

    Step 6: Do not add provider-selective install flags in this phase.

    Tests:
    - CLI install invokes both install paths.
    - CLI output contains `Claude Code hooks: 已安装`.
    - CLI output contains `Codex hooks: 已安装`.
    - Existing Claude settings path options still work.
  </action>
  <verify>
    <automated>pytest src/notifier/tests/test_cli_config.py src/notifier/tests/test_settings.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `notifier-config install` installs both Claude Code and Codex hooks.
    - CLI output is Chinese-first and provider-specific.
    - No split install commands are added.
    - No uninstall behavior is added.
  </acceptance_criteria>
  <done>
    Unified install command installs both providers and reports status in Chinese.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notifier installer -> user Codex config | Installer writes user-level Codex hook configuration. |
| Existing user hooks -> notifier merge | Unrelated hooks must survive notifier install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-031-03 | Tampering | Codex hooks file | mitigate | Remove only notifier-owned entries and preserve unrelated keys/entries. Tests must prove non-destructive merge behavior. |
| T-031-04 | Denial of Service | duplicate hooks | mitigate | Install must be idempotent; running twice must not duplicate notifier entries. |
</threat_model>

<verification>
## Per-Task Verification

- Task 1: `pytest src/notifier/tests/test_settings.py -v --tb=short`
- Task 2: `pytest src/notifier/tests/test_settings.py -v --tb=short`
- Task 3: `pytest src/notifier/tests/test_cli_config.py src/notifier/tests/test_settings.py -v --tb=short`

## Per-Wave Verification

Run:
```powershell
pytest src/notifier/tests/test_settings.py src/notifier/tests/test_cli_config.py -v --tb=short
```

Expected: all targeted tests pass.
</verification>

<success_criteria>

1. Codex hook entries are generated for user-level install.
2. Codex install preserves unrelated settings/hooks.
3. Codex install is idempotent.
4. Unified `notifier-config install` installs both provider hooks.
5. CLI install output reports both provider statuses in Chinese.
6. Codex uninstall remains out of scope.
7. Targeted tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-codex-hook-support-chinese-localization/03.1-02-SUMMARY.md`
</output>
