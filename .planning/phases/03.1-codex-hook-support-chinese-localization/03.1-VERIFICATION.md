---
phase: 03.1-codex-hook-support-chinese-localization
verified: 2026-06-04T00:00:00Z
status: human_needed
score: 22/22 must-haves verified
overrides_applied: 0
overrides: []
re_verification: false
human_verification:
  - test: "Send a Codex hook event (SessionStart, PermissionRequest, Stop) from a real Codex installation and verify the notifier receives and processes it"
    expected: "Events appear in tray history with provider 'Codex', Chinese labels, and correct categories. PermissionRequest fires a Chinese notification; SessionStart and Stop follow the agreed semantics (no notification for SessionStart)"
    why_human: "Requires a working Codex installation with hooks enabled. Cannot simulate without the real Codex agent."
  - test: "Run 'python -m notifier.cli.config install' and verify the CLI output is Chinese-first with both provider statuses"
    expected: "Output shows 'Claude Code hooks: 已安装' and 'Codex hooks: 已安装' with correct file paths"
    why_human: "Verification that CLI renders Chinese correctly in the actual terminal environment (encoding, fonts). Test assertions cover the string content."
  - test: "Trigger a Codex PermissionRequest that produces a Windows desktop notification and verify it shows correct Chinese text with provider source"
    expected: "Toast notification shows title 'Codex - {project}' and body '需要授权 - {message}' with correct Chinese rendering"
    why_human: "Windows toast notification rendering (winotify / WinRT) cannot be verified without a running tray process and a Windows desktop session."
  - test: "Right-click the tray icon with events in history and verify the menu shows Chinese labels with provider source"
    expected: "Menu items show format like 'Codex - permission - 需要授权 (刚刚)'. Empty state shows '暂无事件'. Exit shows '退出'."
    why_human: "pystray system tray menu rendering cannot be inspected programmatically; requires a Windows desktop session."
  - test: "Click an event in the tray menu and verify the detail popup shows Chinese labels with provider source"
    expected: "MessageBox shows '来源', '类别', '时间', '事件', '项目路径' in Chinese with correct provider display"
    why_human: "MessageBoxW window rendering cannot be verified without a running tray process and a Windows desktop session."
  - test: "Verify that unrelated Codex hooks and settings in ~/.codex/hooks.json survive notifier-config install"
    expected: "Non-notifier hook entries remain intact; only notifier-owned entries are managed"
    why_human: "While tested in unit tests with temp directories, real-world verification with actual Codex configuration files ensures no edge cases with the production Codex config format."
---

# Phase 03.1: Codex Hook Support + Chinese Localization Verification Report

**Phase Goal:** Add first-class Codex hook support and Chinese-first user-facing copy before abnormal-state detection.

**Verified:** 2026-06-04
**Status:** human_needed (all 22 programmatic must-haves verified; 6 items require human verification)
**Test Suite:** 192 passed, 0 failed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths — Roadmap Success Criteria

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Notifier events and sessions can distinguish Claude Code and Codex by provider without breaking existing Claude Code behavior | VERIFIED | `Provider` enum (events.py:7-9), `SessionInfo.provider` (events.py:24), `NotifierEvent.provider` (events.py:37), all defaults to `CLAUDE_CODE`. Legacy tests pass unchanged. `_registry_key` includes provider (tcp_server.py:43). |
| 2 | Codex PermissionRequest, Stop, and SessionStart events are normalized into the notifier pipeline with the agreed notification semantics | VERIFIED | `_classify_codex` (events.py:113-134) maps PermissionRequest->PERMISSION, Stop->DONE, SessionStart->IDLE, unknown->ERROR. `should_notify` (notify.py:38-51) suppresses SessionStart and ERROR. Hook CLI passes provider through to classifier. |
| 3 | `notifier-config install` installs both Claude Code and Codex hooks while preserving unrelated user configuration | VERIFIED | `install_codex_hooks` (settings.py:241-297) strips only notifier-owned entries via `_is_notifier_entry`. Unified `install` command (config.py:65-72) calls both installers. Preserves unrelated settings (test_settings.py, test_cli_config.py). |
| 4 | Notifications, tray history, event details, and install/config output use concise Chinese-first copy while keeping provider and technical names recognizable | VERIFIED | Centralized text helpers (text.py) provide Chinese labels. Notifications use "需要授权"/"等待输入"/"任务已完成"/"需要检查". Tray menu uses "暂无事件"/"退出". Detail uses "来源"/"类别"/"时间"/"事件"/"项目路径". CLI uses "已安装". Provider names "Claude Code"/"Codex" stable. |

**Score:** 4/4 roadmap success criteria verified

---

### Plan 01 Must-Haves (Provider-Aware Event Backbone)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Codex is represented as a first-class provider, not a Claude Code compatibility shim (D-01) | VERIFIED | `Provider` enum has `CLAUDE_CODE` and `CODEX` as peers. `_classify_codex` has independent classification rules. |
| 2 | NotifierEvent and SessionInfo both carry provider identity with backward-compatible Claude Code defaults (D-03) | VERIFIED | Both dataclasses have `provider: Provider = Provider.CLAUDE_CODE` (events.py:24,37). Legacy callers without provider work. |
| 3 | Session registry keys include provider, session_id, and cwd to prevent Claude Code/Codex collisions (D-04) | VERIFIED | `_registry_key` returns `f"{provider}||{session_id}||{cwd}"` (tcp_server.py:43). Test `test_codex_and_claude_sessions_dont_collide` proves separation. |
| 4 | Existing notifier categories stay limited to permission, idle, done, and error (D-05) | VERIFIED | `EventCategory` enum has exactly 4 values (events.py:12-16). No new values added. |
| 5 | Codex PermissionRequest maps to permission (D-06) | VERIFIED | `_classify_codex` returns `(EventCategory.PERMISSION, msg)` for PermissionRequest (events.py:121-124). |
| 6 | Codex Stop maps to done (D-07) | VERIFIED | `_classify_codex` returns `(EventCategory.DONE, msg)` for Stop (events.py:126-129). |
| 7 | Codex SessionStart is classified as session-start activity with provider/event metadata preserved (D-08) | VERIFIED | `_classify_codex` returns `(EventCategory.IDLE, "Session started")` for SessionStart, preserving provider and hook_event_name for downstream suppression. |
| 8 | Codex idle is not synthesized without an explicit idle-equivalent event; Phase 4 handles inactivity (D-09) | VERIFIED | No idle synthesis in `_classify_codex`. Only SessionStart, PermissionRequest, and Stop are handled. |
| 9 | Unknown Codex hook events become error records that can be logged/history-visible without notification (D-10) | VERIFIED | Unknown events return `(EventCategory.ERROR, ...)`, and `should_notify` returns False for ERROR category. |

**Score:** 9/9 plan 01 must-haves verified

---

### Plan 02 Must-Haves (Codex Hook Installation + Unified Config CLI)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Codex hooks install at user level by default so all local Codex projects can be monitored (D-15) | VERIFIED | `CODEX_HOOKS_FILE = Path.home() / ".codex" / "hooks.json"` (settings.py:14). |
| 2 | Codex install manages only notifier-owned hook entries and preserves unrelated Codex hooks/settings (D-16) | VERIFIED | `install_codex_hooks` strips entries matching `_is_notifier_entry` before merge (settings.py:270-278). Test `test_preserves_unrelated_codex_settings` and `test_preserves_unrelated_codex_hook_entries` prove preservation. |
| 3 | notifier-config install installs both Claude Code and Codex hooks and reports both statuses (D-17) | VERIFIED | `install()` calls both `install_hooks()` and `install_codex_hooks()` (config.py:65-72). Output shows both provider statuses. |
| 4 | Codex uninstall is not implemented in this phase; leave lifecycle reversal to Phase 5 (D-18) | VERIFIED | No `uninstall_codex_hooks` function or uninstall CLI command exists. |
| 5 | Hook commands use the provider-aware CLI from 03.1-01 and invoke Codex events with provider codex | VERIFIED | `_build_codex_hook_entries` generates commands with `notifier.cli.hook {event} codex` (settings.py:103,114,125). |
| 6 | CLI install/config output is Chinese-first for both providers, using concise status text while keeping provider names stable (D-11,D-12,D-13,D-14) | VERIFIED | Output: "Claude Code hooks: 已安装" and "Codex hooks: 已安装" (config.py:66,71). |

**Score:** 6/6 plan 02 must-haves verified

---

### Plan 03 Must-Haves (Chinese Notifications, Tray, and Detail Provider Display)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | App name remains Claude Code Notifier while event source is displayed as Claude Code or Codex in notifications, history, and details (D-02) | VERIFIED | `app_id="Claude Code Notifier"` (notify.py:107). Notification title format `"Codex - notifier"` (text.py:57-65). Detail window title `"Claude Code Notifier - {project}"` (detail.py:53). |
| 2 | User-visible copy is Chinese-first with no language switch UI or config option in this phase (D-11) | VERIFIED | All display text uses Chinese labels. No language configuration option exists. |
| 3 | Chinese copy covers notifications, tray labels, and event detail popup text; CLI handled by 03.1-02 (D-12) | VERIFIED | Notifications use Chinese via `notification_body` (text.py:68-84). Tray menu uses Chinese labels (menu.py:48,54). Detail uses Chinese labels (detail.py:29-33). CLI was verified in Plan 02. |
| 4 | Provider names, event names, file paths, CLI commands, and technical category identifiers remain recognizable (D-13) | VERIFIED | `provider_label` returns "Claude Code"/"Codex". `event_menu_label` includes category value alongside Chinese label. Detail popup shows both tech name and Chinese label. |
| 5 | Chinese copy is concise tool-style (D-14) | VERIFIED | Labels are short: "需要授权" (5 chars), "等待输入" (4 chars), "任务已完成" (4 chars), "需要检查" (4 chars). |
| 6 | SessionStart and error/unknown Codex events remain non-notifying even after Chinese notification copy is added (D-08,D-10) | VERIFIED | `should_notify` returns False for ERROR and SessionStart regardless of provider (notify.py:46-50). |

**Score:** 6/6 plan 03 must-haves verified

---

### Plan 01 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/notifier/core/events.py` | Provider-aware event model and Codex classification | VERIFIED | `Provider` enum, `SessionInfo.provider`, `NotifierEvent.provider`, `_classify_codex`, all present and substantive |
| `src/notifier/core/session.py` | Provider-aware session extraction | VERIFIED | `extract_session` accepts `provider`, propagates to `SessionInfo` |
| `src/notifier/cli/hook.py` | Provider-aware hook CLI | VERIFIED | `process_hook_event` accepts `provider`, CLI parses it from argv |
| `src/notifier/server/tcp_server.py` | Provider-aware session registry and TCP reconstruction | VERIFIED | `_registry_key` includes provider, `_handle_client` reconstructs provider |

### Plan 02 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/notifier/core/settings.py` | Codex hook installation helpers with non-destructive merge | VERIFIED | `CODEX_HOOKS_FILE`, `install_codex_hooks`, `_build_codex_hook_entries`, all present |
| `src/notifier/cli/config.py` | Unified install command with Chinese status output | VERIFIED | Calls both installers, prints Chinese status for both providers |
| `src/notifier/tests/test_settings.py` | Codex config preservation and idempotency tests | VERIFIED | 36 tests including `TestCodexHookEntriesStructure`, `TestInstallCodexHooks` |
| `src/notifier/tests/test_cli_config.py` | CLI install integration tests | VERIFIED | Created file with 8 tests covering unified install, Chinese output, settings preservation, ownership, idempotency |

### Plan 03 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/notifier/core/text.py` | Centralized Chinese display helpers | VERIFIED | `provider_label`, `category_label`, `notification_title`, `notification_body`, `event_menu_label`, `relative_time_cn` all present |
| `src/notifier/core/notify.py` | Provider-aware Chinese notification formatting and eligibility | VERIFIED | `should_notify`, provider-aware cooldown, delegates to `notification_title`/`notification_body` |
| `src/notifier/tray/menu.py` | Chinese tray menu labels with provider source | VERIFIED | Uses `event_menu_label` and `relative_time_cn`, empty="暂无事件", exit="退出" |
| `src/notifier/tray/detail.py` | Chinese detail popup labels with provider source | VERIFIED | Extracted `_build_detail_body`, Chinese labels, provider visible |
| `src/notifier/tray/app.py` | Chinese tooltip | VERIFIED | Tooltip uses "监控中" instead of "Monitoring" |

All 13 artifacts: exists (Level 1), substantive (Level 2), wired (Level 3), and data flows (Level 4). No stubs, no placeholders.

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `hook.py` | `events.py` | `classify_hook_event(raw, event_type, provider=...)` | WIRED | Import at line 10, called at line 61 |
| `tcp_server.py` | `events.py` | `Provider`, `SessionInfo`, `NotifierEvent` reconstruction | WIRED | Imports at lines 7,71; reconstructs all provider-aware fields |
| `config.py` | `settings.py` | `install_hooks` and `install_codex_hooks` | WIRED | Imports at lines 2-9, called at lines 65,70 |
| `settings.py` | `hook.py` | Hook commands with `notifier.cli.hook {event} codex` | WIRED | Embedded in `_build_codex_hook_entries` command strings |
| `notify.py` | `text.py` | `notification_title` and `notification_body` | WIRED | Imports at line 6, called at lines 105,108 |
| `menu.py` | `text.py` | `event_menu_label` and `relative_time_cn` | WIRED | Imports at line 3, called at lines 13-14 |
| `detail.py` | `text.py` | `provider_label` and `category_label` | WIRED | Imports at line 3, called at lines 13,19 |

All 7 key links verified as WIRED.

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `notify.py:dispatch_notification` | `notification_title(event)` | `text.py:57-65` — reads `event.provider` and `event.session.project_name` | Yes — uses live event fields | FLOWING |
| `notify.py:notification_body` | `notification_body(event)` | `text.py:68-84` — reads `event.category` and `event.message` | Yes — uses live event fields | FLOWING |
| `menu.py:_format_event` | `event_menu_label(event, relative_time)` | `text.py:87-104` — reads `event.provider`, `event.category`, and time string | Yes — uses live event fields and computed relative time | FLOWING |
| `detail.py:_build_detail_body` | `provider_label(event.provider)`, `category_label(event.category)` | `text.py:13-34,37-54` — reads event provider and category | Yes — uses live event fields | FLOWING |
| `app.py:patched_update` | `session_registry` count for tooltip | `NotifierServer.session_registry` | Yes — updated on each event | FLOWING |

All wired artifacts have data flowing from real sources. No hardcoded empty values in the data path.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Provider enum values | `python -c "from notifier.core.events import Provider; ..."` | CLAUDE_CODE="claude_code", CODEX="codex" | PASS |
| EventCategory enum values | `python -c "from notifier.core.events import EventCategory; ..."` | permission, idle, done, error | PASS |
| Chinese provider labels | `python -c "from notifier.core.text import provider_label; ..."` | "Claude Code", "Codex" | PASS |
| Chinese category labels | `python -c "from notifier.core.text import category_label; ..."` | 需要授权, 等待输入, 任务已完成, 需要检查 | PASS |
| Codex hook entries structure | `python -c "from notifier.core.settings import CODEX_HOOK_ENTRIES; ..."` | All 3 events, all include "codex" provider | PASS |
| Full test suite | `python -m pytest src/notifier/tests/ -v --tb=short` | 192 passed, 0 failed | PASS |

---

### Probe Execution

No probes defined for this phase (not a migration or tooling phase). Skipped.

---

### Requirements Coverage

This phase has `requirements: TBD` in ROADMAP.md and `requirements: []` in all three plan frontmatters. REQUIREMENTS.md does not map any REQ-IDs to this phase. The phase is an inserted phase between Phase 3 and Phase 4, extending the system with Codex support and Chinese localization without formal requirement IDs.

No orphaned or blocked requirements to report.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/notifier/tests/test_tray_app.py` | 14 | "placeholder" in docstring | INFO | Old docstring from Phase 2. The icon is fully implemented (not a placeholder). No code impact. |

No TBD, FIXME, XXX, TODO, HACK markers found in any source file. No empty implementations. No hardcoded empty data flowing to user-visible output.

---

### Git Commit Verification

| Summary | Commits |
|---------|---------|
| 03.1-01-SUMMARY.md | ca32ce8, 2100bd2, d0111b1, 08783ed — all verified in git log |
| 03.1-02-SUMMARY.md | 36298db, 0b306b1, 819e9a1 — all verified in git log |
| 03.1-03-SUMMARY.md | a2fda6c, 5485a66, dfb784d — all verified in git log |

All 9 commits confirmed present in repository history.

---

### Human Verification Required

All programmatic must-haves (22/22) are verified. The following items require a human to confirm on a Windows system with a running notifier tray instance and optionally a Codex installation.

#### 1. End-to-End Codex Hook Integration

**Test:** Send a Codex hook event (SessionStart, PermissionRequest, Stop) from a real Codex installation and verify the notifier receives and processes it.
**Expected:** Events appear in tray history with provider "Codex", Chinese labels, and correct categories. PermissionRequest fires a Chinese notification; SessionStart does not notify.
**Why human:** Requires a working Codex installation with hooks enabled. Cannot simulate without the real Codex agent.

#### 2. CLI Install Output

**Test:** Run `python -m notifier.cli.config install` and verify the CLI output renders correctly.
**Expected:** Output shows `Claude Code hooks: 已安装 (/path/to/settings.json)` and `Codex hooks: 已安装 (/path/to/hooks.json)`.
**Why human:** Terminal encoding/font rendering of Chinese characters. Test assertions cover the string content but not terminal display.

#### 3. Windows Desktop Notification Display

**Test:** Trigger a Codex PermissionRequest that produces a Windows desktop notification (requires running tray process).
**Expected:** Toast notification shows title `Codex - {project}` and body `需要授权 - {message}` with correct Chinese rendering.
**Why human:** winotify/WinRT toast rendering cannot be verified without a running tray process and a Windows desktop session.

#### 4. Tray Menu Chinese Labels

**Test:** Right-click the tray icon with events in history and verify the menu labels.
**Expected:** Menu items show format like `Codex - permission - 需要授权 (刚刚)`. Empty state shows "暂无事件". Exit shows "退出".
**Why human:** pystray system tray menu rendering cannot be inspected programmatically; requires a Windows desktop session.

#### 5. Event Detail Popup

**Test:** Click an event in the tray menu and verify the detail popup.
**Expected:** MessageBox shows "来源", "类别", "时间", "事件", "项目路径" in Chinese with correct provider display.
**Why human:** MessageBoxW window rendering cannot be verified without a running tray process and a Windows desktop session.

#### 6. Real-World Config Preservation

**Test:** Install hooks with `notifier-config install` on a system that has existing, real Codex configuration in `~/.codex/hooks.json`.
**Expected:** Non-notifier hook entries remain intact; only notifier-owned entries are managed.
**Why human:** Unit tests cover this with synthetic data, but real-world Codex config formats may have edge cases not captured in test fixtures.

---

### Gaps Summary

No gaps found. All 22 must-have truths across the 3 plans are verified against the codebase. All 4 roadmap success criteria are met. All 13 required artifacts exist, are substantive, wired, and have data flowing. All 7 key links are connected. No debt markers (TBD, FIXME, XXX) found in source code. Full test suite passes (192/192).

The only remaining items are the 6 human verification checks listed above, covering visual appearance (notifications, tray menu, detail popup), end-to-end Codex integration, and real-world config preservation. These are inherent to Windows GUI applications and external tool integration.

---

_Verified: 2026-06-04T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
